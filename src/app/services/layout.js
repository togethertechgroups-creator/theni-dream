export const metadata = {
  title: "Our Services | Theni Dream Photography",
  description: "Browse the 11 photography services offered by Theni Dream Photography. From weddings, candid, maternity and baby shoots, to corporate events and drone coverage.",
};

export default function ServicesLayout({ children }) {
  return <>{children}</>;
}
